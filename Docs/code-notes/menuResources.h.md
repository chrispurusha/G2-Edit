# menuResources.h notes

The longer comments from `menuResources.h`, moved here 2026-09-12 so the code reads cleanly. The code points at each as `// notes §k`. Verbatim and in file order; each is titled by what it documents.

## 1. `gMidiChanItems`


Static menu item arrays for synth settings dropdowns.
Included only by menus.c, after action_setting_u8 / action_setting_i8 are defined.
