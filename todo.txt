G2-Edit TODO (priority order)

- protocol.c:1670 — master volume is hardcoded (0x50) instead of read from gMasterVolume
- protocol.c:1697 — write-side patch-settings parsing needs rewriting to match read-side logic in usbComms.c
- graphics.cpp:752-753 — perf-mode write path relies on a blind 2-second usleep instead of real state confirmation
- usbComms.c — centralize repeated unsolicited send_stop() calls (lines ~3590-3781) instead of scattering them
- usbComms.c — resolve perf-mode ambiguities: synth vs. perf ownership (line 342), voice-count ordering on increase (line 576/582), unconfirmed message case (line 903)
- graphics.cpp:738 — move parse_patch to a shared file so both USB and file-load paths can use it
- moduleGraphics.cpp — refactor volume-meter/label rendering into utilsGraphics.cpp; fix upRate flag being applied too broadly (should be audio-connector-only, line 540); resolve "can Mode be morphed?" question (line 387)
- OscA(97) — add new UI element for Freq control (param 0) that accounts for param 6 Pitch Type
- Sw8-1(15), ValSw1-2(17) — need more resources
- OscShpB(8) — check mode handling
- Fix known caveats on otherwise-OK modules: Oscar(9) mode, EnvADSR(20) values, PartQuant(22) dial range (0 to +/-63), LfoC(24) Range Sub param, SeqNote/SeqEvent/SeqVal/SeqLev park LED handling, MixFader(161) value scale (match Mix4-1C's 0-100/xx.x mapping)
- Verify remaining 117 unverified module types (filters, oscillators, delay lines, sequencer/logic, MIDI send/receive, Resonator, Operator, Random* family, etc.) — see "Module dev debug notes.txt"
- Add performance keyboard split/layer/zone UI (manual's "Layering Patches" section) — currently only the standalone NoteZone module exists
- Add a dedicated master-clock/tempo panel (currently only basic send/receive clock toggles exist)
- globalVars.h:63,70,71 — nullify knob/controller arrays on new patch load
- msgQueue.h:284 — remove unused mutex
- defs.h:162 — confirm whether perf-header sub-response is ever actually used
- rationalise architecture and commonalise components for other projects
  - Add a generic hit-rectangle registry to SynthLib (hitRegistry.h/.c): tHitId keyed off existing tModuleKey/tCableKey convention (make_module_hit_id), hit_registry_begin_frame/register/find/get, flat fixed-capacity array cleared per frame — modeled on immediate-mode GUI hit-testing (Dear ImGui style) rather than per-project fixed-size arrays
  - Migrate G2-Edit's gParamRectangle[slot][location][index][param] and other named hit rects (gPatchParamClose, gTopbarControls[].rectangle, etc.) onto the SynthLib registry, module-by-module, replacing direct array indexing with register/get calls
  - Once mouse hit-testing is decoupled from render-time global arrays, generalize mousePanels.c/mouseTopbar.c/selection.c into shared SynthLib components so Z1-Edit/EmuUtility can adopt them instead of maintaining independent mouseHandle.c implementations
  - Evaluate generalizing the msgQueue.c + usbComms.c thread/message-queue pattern into SynthLib as a transport-agnostic comms layer (USB vs MIDI as pluggable backend), since Z1-Edit/EmuUtility currently talk to MIDI directly with no shared queue abstraction
  - Evaluate extracting menus.c's declarative build/dispatch mechanism into SynthLib (item tables stay per-project; G2-Edit's menus.c is 2186 lines vs 114 in Z1-Edit/EmuUtility with ~100% divergence)
  - Finish moving G2-Edit's remaining imperative layout math into moduleResources.h-style declarative entries (e.g. render_volume_common's inline coord stepping, one-off globals like gKnobOverlayRect) so position data is consistently table-driven, not computed ad hoc
  - Design one shared descriptor schema (control type, rect+anchor, range/default, string-map ref, colour-map ref) reusable as both a compile-time C table (G2-Edit) and a runtime-parsed file (Z1-Edit) — same shape, swappable loader — rather than Z1 growing a divergent format
  - Prototype a runtime-parsed panel description for Z1-Edit as the pilot (single fixed panel, no dynamic module instantiation like G2), replacing z1Graphics.cpp's named-global dial rects and inline literal coords
  - Land the anchor-based layout resolver (adjust_rectangle equivalent) alongside the hit-registry in SynthLib, so both G2's compile-time tables and Z1's runtime-parsed descriptions feed the same generic panel engine
  - Add page-selection tabs to Z1-Edit's top bar (e.g. Synth, Effects; split further e.g. Oscillators/Filters if a page runs out of room) — generalize G2-Edit's existing topbarControls.def X-macro + set_exclusive_button_highlight() pattern (currently used for the VA/FX toggle and slot A-D tabs) into a shared SynthLib "page tab row" concept instead of reimplementing it in Z1
  - Represent pages as a grouping construct in the shared descriptor schema (page = label + hit id + set of controls); only the active page's controls get rendered/registered per frame, which the hit-registry's per-frame clear already gives for free
